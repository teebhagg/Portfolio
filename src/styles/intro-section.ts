import { SxProps, Theme } from "@mui/material";

export const container_style_default: SxProps<Theme> = { display:'flex', justifyContent: 'space-between', alignItems:'center', gap: '20px' }

export const container_style_mobile: SxProps<Theme> = { display:'flex', flexDirection:'column', }